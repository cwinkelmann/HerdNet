wandb_notes: |
  "default Settings from From crowd to herd counting on aed with timm Dinov2_base model"
  "full image training with random crop, all augmentations which were used in iguana"
  "crop image evaluation"

defaults:
  - /losses: herdnet_publication
  - /datasets: aed_train_full_size_eval_crop_aug_all
  - /training_settings: delplanque2022_val_loss
  - /model: Dinov2_base
  - _self_  # Include this config's content

wandb_flag: True
wandb_project: 'herdnet_aed'
wandb_entity: 'karisu'
wandb_run: 'DinoV2_base_train_full_eval_crop_aug_all'
wandb_tags: ['DINOv2', 'aed','Dinov2_base', 'default_setting', "exp1", "eval_crop", "train_full", "aug_all"]
seed: 1
device_name: null

#model:
#  load_from: /raid/cwinkelmann/herdnet/outputs/2025-09-12/10-40-54/best_loss_model.pth # dla34 dr2 - https://wandb.ai/karisu/herdnet_aed/runs/70nu9hbg/logs
#  kwargs:
#    down_ratio: 4
training_settings:
  batch_size: 20
  lr: 0.00001
#  evaluator:
#    kwargs:
#      lmds_kwargs:
#        scale_factor: 8 # # dr=2 -> scale_factor 16, dr=4 -> 8, dr=8 -> 4, dr=16 -> 2 in the non patching case

hydra:
  run:
    dir: /raid/cwinkelmann/herdnet/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

