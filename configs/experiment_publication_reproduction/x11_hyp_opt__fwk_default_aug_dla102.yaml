wandb_notes: |
  Trying to solve Fer_FWK using default augmentaions 
  

defaults:
  - /losses: herdnet_publication
  - /datasets: All_detection_extend_default_aug
  - /training_settings: delplanque2022_evaluator_stitcher
  - /model: HerdnetTimmDLA102_dr2_imagenet
  - _self_  # Include this config's content

wandb_flag: True
wandb_project: 'x11_hyp_opt__fwk_hypopt'
wandb_entity: 'karisu'
wandb_run: 'x11_hyp_opt__fwk_default_aug_dla102'
wandb_tags: ['dla', 'dla_102', 'aug_default', "exp11", "eval_crop", "train_crop", "corrected"]
seed: 1
device_name: null


training_settings:
  lr: 1.0e-04
  epochs: 150
  batch_size: 15


hydra:
  run:
    dir: /raid/cwinkelmann/herdnet/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

