wandb_notes: |
  "using more aggressive augmentations for training"

defaults:
  - /losses: herdnet_publication
  - /datasets: iguana_cvat_corr_crop_train_crop_val_aug_all
  - /training_settings: delplanque2022_evaluator_stitcher
  - /model: Dinov2_large_best
  - _self_  # Include this config's content

wandb_flag: True
wandb_project: 'herdnet_iguana_cvat_corr'
wandb_entity: 'karisu'
wandb_run: 'x8_iguana_cvat_corr_dinoL_pub_train_tile_eval_tile_aug_all'
wandb_tags: ['dla', 'dinoL', 'default_setting', "exp8", "eval_crop", "train_crop", "corrected", "aug_all"]
seed: 1
device_name: null


# overrides
# overrides
training_settings:
  lr: 1.0e-06
  epochs: 25
  batch_size: 5
model:
#     dinoL
    load_from: /raid/cwinkelmann/herdnet/outputs/2025-09-29/08-45-18/best_model.pth # https://wandb.ai/karisu/herdnet_iguana_cvat_corr/runs/4a7n15xj/logs
#datasets:
#  validate:
#    albu_transforms:
#      ObjectAwareRandomCropEdgeBlackout:
#        p: 0.0

hydra:
  run:
    dir: /raid/cwinkelmann/herdnet/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

