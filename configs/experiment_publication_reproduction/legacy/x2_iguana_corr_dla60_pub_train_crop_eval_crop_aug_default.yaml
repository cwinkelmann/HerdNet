wandb_notes: |
  "default Settings from From crowd to herd counting on aed with timm DLA34 model"

defaults:
  - /losses: herdnet_publication
  - /datasets: floreana_crop_train_corrected_crop_val_corrected_aug_default
  - /training_settings: delplanque2022_evaluator
  - /model: HerdnetTimmDLA60_dr2_imagenet
  - _self_  # Include this config's content

wandb_flag: True
wandb_project: 'herdnet_iguana'
wandb_entity: 'karisu'
wandb_run: 'x2_iguana_corr_dla60_pub_train_crop_eval_crop_aug_default'
wandb_tags: ['floreana','dla', 'dla60', 'default_setting', "exp1", "eval_crop", "train_crop", "corrected", "aug_default"]
seed: 1
device_name: null

model:
  load_from: /raid/cwinkelmann/herdnet/outputs/2025-09-14/05-06-17/best_model.pth # dla60 dr2 - https://wandb.ai/karisu/herdnet_iguana/runs/h5gvw9vw/overview
training_settings:
  batch_size: 40

#  kwargs:
#    down_ratio: 4
#training_settings:
#  batch_size: 4
#  evaluator:
#    kwargs:
#      lmds_kwargs:
#        scale_factor: 8 # # dr=2 -> scale_factor 16, dr=4 -> 8, dr=8 -> 4, dr=16 -> 2 in the non patching case

hydra:
  run:
    dir: /raid/cwinkelmann/herdnet/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

