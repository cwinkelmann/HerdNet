wandb_notes: |
  "using more aggressive augmentations for training"

defaults:
  - /losses: herdnet_publication
  - /datasets: iguana_cvat_class_crop_train_crop_val_aug_all
  - /training_settings: delplanque2022_evaluator_stitcher
  - /model: HerdNetTimmDLA34_dr2_imagenet
  - _self_  # Include this config's content

wandb_flag: True
wandb_project: 'herdnet_iguana_cvat_corr'
wandb_entity: 'karisu'
wandb_run: 'x8_iguana_cvat_class_dla34_pub_train_tile_eval_tile_aug_all'
wandb_tags: ['dla', 'dla_34', 'default_setting', "exp8", "eval_crop", "train_crop", "not_corrected"]
seed: 1
device_name: null


# overrides
# overrides
training_settings:
  lr: 1.0e-04
  epochs: 250
  batch_size: 5
model:
#     dla34 dr2
    load_from: /raid/cwinkelmann/herdnet/outputs/2025-09-28/22-39-03/best_model.pth # https://wandb.ai/karisu/herdnet_iguana_cvat_corr/runs/m8m8v5xt/logs
#datasets:
#  validate:
#    albu_transforms:
#      ObjectAwareRandomCropEdgeBlackout:
#        p: 0.0

hydra:
  run:
    dir: /raid/cwinkelmann/herdnet/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

